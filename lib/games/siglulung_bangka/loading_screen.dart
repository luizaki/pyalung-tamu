import 'package:flutter/material.dart';

class BangkaLoadingScreen extends StatelessWidget {
  const BangkaLoadingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('WIP'),
      ),
    );
  }
}
