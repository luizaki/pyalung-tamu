import 'package:flutter/material.dart';

class TugakLoadingScreen extends StatelessWidget {
  const TugakLoadingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('WIP'),
      ),
    );
  }
}
